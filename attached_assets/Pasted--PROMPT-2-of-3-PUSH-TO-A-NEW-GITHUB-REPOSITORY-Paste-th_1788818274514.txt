# PROMPT 2 of 3 — PUSH TO A NEW GITHUB REPOSITORY

> Paste this into Replit only after Prompt 1's fixes are complete and confirmed.

---

## 1. PRE-PUSH QUALITY CHECK (DO THIS BEFORE PUSHING ANYTHING)

Before pushing any code, do a full verification pass and confirm all of the following are true:

- The project builds successfully from a clean install (no cached state) with no build errors.
- No console errors or warnings appear on any page at runtime.
- No broken imports, missing dependencies, or dead code paths.
- No broken links, broken images, or broken placeholder assets anywhere on the site.
- Every fix from the most recent round (preloader, forms, animations, rectangular shapes, pagination) is actually present in the code being pushed — not left on an unsaved/uncommitted branch.
- The `.gitignore` correctly excludes `node_modules`, any build output folders, and any `.env`/secrets files — confirm no environment variables or API keys are present in the source code itself.

Report back explicitly on each of these before proceeding to the push.

---

## 2. CREATE AND PUSH TO A NEW GITHUB REPOSITORY

This should go to a **new** repository under Ines's GitHub account (`https://github.com/InesMlh`) — not her existing `Portfolio` or `PortfolioV2` repositories, and not overwrite either of them.

1. Ask Ines to confirm the exact name she wants for the new repository before creating it (suggest something clear like `Portfolio-Live` or `Portfolio-Final` if she doesn't have a preference, but don't decide for her).
2. Initialize git in the project if not already initialized.
3. Connect Replit's GitHub integration and authorize access to her account if not already connected.
4. Create the new repository (public or private — ask Ines which she prefers; note that a private repo works fine with Vercel but a public one is simplest if she ever wants to share the code).
5. Commit all current code with a clear, descriptive commit message summarizing the state of the project (e.g., "Portfolio v6 — preloader restored, forms fixed, animation and shape fixes").
6. Push to the new repository's main branch.

---

## 3. CONFIRM AFTER PUSHING

After the push completes:

- Confirm the repository is visible and complete on GitHub (spot-check that key files/folders are present).
- Give Ines the exact repository URL.
- Give her plain, step-by-step instructions for how to pull future changes from Replit into this same repository going forward, and how to reconnect to it from a fresh Replit session if she ever needs to.